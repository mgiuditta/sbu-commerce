# Database (per-service, override DB_NAME in each service)
DB_HOST=localhost
DB_PORT=5432
DB_USER=sbu
DB_PASSWORD=sbu_secret
DB_NAME=sbu_catalog

# NATS
NATS_URL=nats://localhost:4222

# Service
PORT=3000
NODE_ENV=development
